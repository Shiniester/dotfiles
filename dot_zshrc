ZSH_DIR="$HOME/.config/zsh"

# 2. 按顺序加载模块
for zsh_file in \
  "init.zsh" \
  "themes.zsh" \
  "completion.zsh" \
  "plugins.zsh" \
  "input.zsh" \
  "functions.zsh" \
  "aliases.zsh"; do
  [[ -r "${ZSH_DIR}/${zsh_file}" ]] && source "${ZSH_DIR}/${zsh_file}"
done
